# Dev proxy: http://localhost:8080 → Vite dev server (HMR) + Django for API paths.
:80 {
	encode zstd gzip

	@backend path /api/* /oauth/* /admin-django/* /static/*
	handle @backend {
		reverse_proxy backend:8000
	}

	handle {
		reverse_proxy frontend:5173
	}
}
